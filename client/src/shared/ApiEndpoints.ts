export namespace Endpoints {
    export namespace User {
        export const self = `/api/users/user`
    }
    export namespace Notifications {
        export const all = `/api/notifications/all`
    }
}